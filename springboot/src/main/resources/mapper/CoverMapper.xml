<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.springboot.mapper.SysFileInfoMapper">

    <select id="selectCoverById1">
        SELECT EXISTS(SELECT * FROM activity WHERE cover_file_id = #{id})AS is_exist;
    </select>

    <delete id="deleteCoverById">
        UPDATE activity SET cover_file_id = NULL WHERE cover_file_id = #{id}
    </delete>
    <delete id="deleteCoverById2">
        UPDATE course SET cover_file_id = NULL WHERE cover_file_id = #{id}
    </delete>
</mapper>

