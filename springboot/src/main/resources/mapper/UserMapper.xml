<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.springboot.mapper.UserMapper">
    <!-- Query user with a derived new-user flag while keeping BaseMapper CRUD intact. -->
    <select id="getUserWithNewFlagById" resultType="org.example.springboot.entity.User">
        SELECT
            u.*,
            IF(
                TIMESTAMPDIFF(HOUR, u.created_at, NOW()) &lt;= 24
                OR
                (
                    (SELECT COUNT(*) FROM activity_signup s WHERE s.user_id = u.id) = 0
                    AND (SELECT COUNT(*) FROM ai_chat_session c WHERE c.user_id = u.id) &lt;= 1
                    AND (SELECT COUNT(*) FROM quiz_record q WHERE q.user_id = u.id) = 0
                    AND (SELECT COUNT(*) FROM shop_order o WHERE o.user_id = u.id) = 0
                ),
                1, 0
            ) AS is_new_user
        FROM `user` u
        WHERE u.id = #{userId}
    </select>
</mapper>
